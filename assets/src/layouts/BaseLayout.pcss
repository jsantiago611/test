.BaseLayout {

  body {
    min-height: 100vh;
    display: grid;
    grid-template-rows: auto 1fr auto;
  }

  &__main {
    padding: 4rem var(--container-gutter);
    font-size: var(--body-copy-size);
  }
}
